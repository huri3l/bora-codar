import { PlayerScreen } from '@/components/01-player/PlayerScreen';

const Player = () => {
  return (
    <main className="w-screen h-screen bg-[#0F0D13] flex justify-center items-center">
      <PlayerScreen />
    </main>
  );
};

export default Player;
